rules:
  - denyRule:
      deniedPrincipals:
        - "principalSet://goog/public:all"

      exceptionPrincipals:
        - "principal://goog/subject/__ADMIN_EMAIL__"

      deniedPermissions:

      # VM Protections
      # - "compute.googleapis.com/instances.create"
        - "compute.googleapis.com/instances.setLabels"
       
